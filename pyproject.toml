[project]
name = "colorscheme-generator"
version = "0.1.0"
description = "Colorscheme generator using a image reference"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pillow>=12.2.0",
    "rich>=15.0.0",
]
